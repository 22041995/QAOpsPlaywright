# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: Network1.spec.js >> Security test request interception
- Location: tests\Network1.spec.js:3:1

# Error details

```
Test timeout of 50000ms exceeded.
```

```
Error: expect(locator).toHaveText(expected) failed

Locator:  locator('p').last()
Expected: "You are not authorize to view this order"
Received: "Automation Practice"

Call log:
  - Expect "toHaveText" with timeout 50000ms
  - waiting for locator('p').last()
    5 × locator resolved to <p _ngcontent-hxi-c38="">Automation Practice</p>
      - unexpected value "Automation Practice"

```

```yaml
- paragraph: Automation Practice
```

# Test source

```ts
  1  | const { test , expect } = require('@playwright/test');
  2  | 
  3  | test('Security test request interception', async ({ page }) => {
  4  | 
  5  |     await page.goto("https://rahulshettyacademy.com/client/#/auth/login");
  6  |     const userEmail = page.locator('#userEmail');
  7  |     const password = page.locator('#userPassword');
  8  |     await userEmail.fill("nemishra2295@gmail.com");
  9  |     await password.fill("22041995@April");
  10 |     await page.locator("#login").click();
  11 |     await page.waitForLoadState('networkidle');
  12 |     await page.locator(".card-body b").first().waitFor();
  13 | 
  14 |     await page.locator("button[routerlink*='myorders']").click();
  15 |     await page.route("https://rahulshettyacademy.com/api/ecom/order/get-orders-details?id=*",
  16 |         route => route.continue({
  17 |             url:
  18 |                 "https://rahulshettyacademy.com/api/ecom/order/get-orders-details?id=6a2d908517ee3e78bad1331e"
  19 |         }));
  20 |     await page.locator("button:has-text('View')").first().click();
> 21 |     await expect(page.locator("p").last()).toHaveText("You are not authorize to view this order");
     |                                            ^ Error: expect(locator).toHaveText(expected) failed
  22 | })
```